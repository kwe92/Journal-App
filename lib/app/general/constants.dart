/// PrefKeys: shared preference keys.
class PrefKeys {
  const PrefKeys._();

  static const accessToken = "jwt";
}

class MediaType {
  const MediaType._();
  static const json = "application/json";
}
